/**
 * Entry point for the Playwright Vision Reporter
 * This allows users to import the reporter as 'playwright-vision-ai-debugger/reporter'
 */

// Just re-export from the reporter module
module.exports = require('./dist/reporter/reporter');